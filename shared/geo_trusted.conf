geo $is_trusted_proxy {
    default         0;
    172.20.0.11     1;   # nginx1
    172.20.0.12     1;   # nginx2
    172.20.0.13     1;   # nginx3
}

map $is_trusted_proxy $xff_value {
    1       "$http_x_forwarded_for, $remote_addr";
    default "$remote_addr";
}
